<div id="<%= dom_id pin %>" class="rounded shadow m-3 p-3">
  <p>
    <strong>Title:</strong>
    <%= pin.title %>
  </p>
  <%= pin.body %>
  <br/>
  <hr/>
  <% pin.pictures.each do |picture| %>
    <%= link_to image_tag(picture), picture %>
 <% end %>
</div>
